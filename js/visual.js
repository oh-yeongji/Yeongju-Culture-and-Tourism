document.addEventListener("DOMContentLoaded", function () {
  // 페이지 로드 후 실행될 코드

  // 요소 선택
  var progressBar = document.querySelector(".count .progressBar .bar");

  // 클래스 추가
  progressBar.classList.add("on");
});
